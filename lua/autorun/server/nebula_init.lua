--[[
    Nebula Framework - Server Autorun
    Ensures the framework loads correctly
]]

if not NEBULA_LOADED then
    NEBULA_LOADED = true
    MsgC(Color(100, 180, 255), "[Nebula] ", color_white, "Server autorun loaded.\n")
end
